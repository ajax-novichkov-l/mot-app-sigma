{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "... at now I'm trying do something",
            "type": "shell",
            "command": "bash",
            "args": ["-c", 
            "cd \"/mnt/Ubuntu_Data/doorbell/vms-firmware/output/build/mot-app\" && ls && make clean && make"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}